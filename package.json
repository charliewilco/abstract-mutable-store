{
	"name": "@charliewilco/abstract-mutable-store",
	"version": "0.1.0",
	"repository": "charliewilco/abstract-mutable-store",
	"license": "Unlicense",
	"files": [
		"dist/**/*"
	],
	"main": "dist/index.cjs",
	"module": "dist/index.mjs",
	"types": "dist/index.d.ts",
	"typings": "dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.mjs",
			"require": "./dist/index.cjs"
		}
	},
	"sideEffects": false,
	"scripts": {
		"prepare": "npm run build",
		"clean": "rm -rf dist .tmp",
		"prebuild": "npm run clean",
		"build": "npm run build:types && rolldown -c",
		"build:types": "tsc -p tsconfig.build.json",
		"typecheck": "npm run typecheck:src && npm run typecheck:test",
		"typecheck:src": "tsc -p tsconfig.build.json --noEmit",
		"typecheck:test": "tsc -p tsconfig.test.json --noEmit",
		"pretest": "rm -rf .tmp/test && tsc -p tsconfig.test.json",
		"test": "node --test .tmp/test/*.test.js",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"check": "npm run format:check && npm run typecheck && npm test && npm run build"
	},
	"devDependencies": {
		"@types/node": "^24.12.4",
		"prettier": "^3.8.3",
		"rolldown": "^1.0.3",
		"typescript": "^6.0.3"
	},
	"prettier": {
		"useTabs": true,
		"printWidth": 95,
		"bracketSameLine": true
	}
}
